
# https://arduino.github.io/arduino-cli/0.19/commands/arduino-cli_compile/

# arduino-cli compile -b BOARD --build-path PATH --clean SKETCHPATH


# arduino-cli core update-index
# arduino-cli core upgrade

# git -C ~/Arduino/libraries/MIC184_Temperature_Sensor pull --ff-only
# git -C ~/Arduino/libraries/SSD1322_for_Adafruit_GFX pull --ff-only
# arduino-cli lib update-index
# arduino-cli lib upgrade

# wie und wo ablegen, stable und testing?, einen hook für github/MiSTer_tty2oled
